#' Simple mapping tool
#'
#' @param x
#'
#'
#'
#'
smoov = function(x){
  # Check if shapefile is loaded. If not then load and store in smoov
  # environment
  if(!exists(.smoov_env)){
    .smoov_env = new.env()
  }
}